# 🧵 Threads Copilot Bot

Telegram-бот для генерации вирусных постов в Threads.  
AI-помощник, который кидает короткие хуки, идеи и наброски — по одной команде.

## Возможности

- 🎯 **Генерация хуков** — 1-2 предложения с крючком и провокацией
- 📝 **Варианты** — 2-3 коротких наброска на тему
- 📄 **Полные посты** — по запросу «напиши пост»
- 💬 **Чат-слоты** — до 5 чатов с разными стилями генерации
- 🧠 **Контекст** — помнит предыдущие сообщения (до 10 пар)
- 👑 **Pro-статус** — безлимитные запросы (выдаёт админ)

## Технологии

| Компонент | Технология |
|---|---|
| Бот-фреймворк | aiogram 3.x |
| AI-модель | Claude Sonnet 4.6 (via OpenRouter) |
| База данных | SQLite + SQLAlchemy 2.0 (async) |
| Конфигурация | pydantic-settings + .env |

## Структура проекта

```
├── main.py              # Entry point, Dispatcher
├── config.py            # Settings from .env
├── requirements.txt     # Dependencies
├── database/
│   ├── models.py        # User, ThreadsProfile (ORM)
│   └── session.py       # Async engine & session
├── handlers/
│   ├── user.py          # User commands & FSM
│   └── admin.py         # /admin_promote
└── services/
    ├── ai.py            # OpenRouter API + system prompt
    └── usage.py         # Freemium billing logic
```

## Установка

```bash
# Клонировать
git clone https://github.com/<your-username>/threads-copilot-bot.git
cd threads-copilot-bot

# Зависимости
pip install -r requirements.txt

# Настроить .env
cp .env.example .env
# Заполнить BOT_TOKEN, OPENROUTER_API_KEY, ADMIN_IDS
```

## Запуск

```bash
python main.py
```

Бот стартует в режиме long-polling. БД создаётся автоматически.

## Команды бота

| Команда | Описание |
|---|---|
| `/start` | Запуск, меню чатов |
| `/chats` | Список чат-слотов |
| `/switch` | Сменить активный чат |
| `/clear` | Очистить контекст |
| `/pro_status` | Статус подписки |
| `/admin_promote <id>` | Выдать Pro (только админ) |

## Переменные окружения

| Переменная | Обязательная | Описание |
|---|---|---|
| `BOT_TOKEN` | ✅ | Токен от @BotFather |
| `OPENROUTER_API_KEY` | ✅ | Ключ OpenRouter API |
| `ADMIN_IDS` | ❌ | Telegram ID админов (через запятую) |
| `DATABASE_URL` | ❌ | Путь к БД (default: `sqlite+aiosqlite:///./bot.db`) |
| `DAILY_FREE_LIMIT` | ❌ | Лимит запросов/день (default: 5) |

## Лицензия

MIT
